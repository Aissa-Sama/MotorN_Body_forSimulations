// tests/test_binary_orbit.cpp
void test_kepler_orbit() {
    // Crear sistema binario en órbita circular
    // Integrar por varios períodos
    // Verificar que la energía se conserva (dE/E < 1e-6)
    // Verificar que el período es correcto (T = 2π√(a³/GM))
}

// tests/test_hybrid_integrator.cpp
void test_binary_with_field() {
    // Binaria + estrella lejana
    // Verificar que la binaria mantiene su ligadura
    // Verificar que el campo siente la masa total de la binaria
}